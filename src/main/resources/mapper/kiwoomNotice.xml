<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.KiwoomNoticeMapper">

       <select id="kiwoomNoticeList" parameterType="com.vo.KiwoomNoticeVO" resultType="com.vo.KiwoomNoticeVO">
              select board_no,
              emp_no,
              board_title,
              board_content,
              reg_date,
              board_hit,
              mod_date
              from media_board
              <where>
                     <if test="board_no!=0">AND board_no=#{board_no}</if>
                     <!--           <if test="gubun!=null">-->
                     <!--             <choose>-->
                     <!--                 <when test='gubun.equals("board_title")'>-->
                     <!--                AND board_title LIKE '%'||#{keyword}||'%'-->
                     <!--            </when>-->
                     <!--            <when test='gubun.equals("board_no")'>-->
                     <!--                AND board_no LIKE '%'||#{keyword}||'%'-->
                     <!--            </when>-->
                     <!--            <when test='gubun.equals("emp_no")'>-->
                     <!--                AND emp_no LIKE '%'||#{keyword}||'%'-->
                     <!--            </when>-->
                     <!--        </choose>-->
                     <!--    </if>-->
              </where>
              --     order by board_no desc
       </select>

       <delete id="kiwoomNoticeDelete" parameterType="int">
              delete from media_board where board_no= #{board_no}
       </delete>

       <insert id="kiwoomNoticeInsert" parameterType="map">
              insert into media_board(board_no,board_title,board_content,emp_no,reg_date,board_hit,mod_date)
              values(board_pk_seq.nextval,
                     #{board_title},
                     #{board_content},
                     1010,
                     '2000-00-00',
                     0,
                     '2000-00-00')
       </insert>
</mapper>