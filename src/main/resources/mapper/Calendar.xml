<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgw.ticket.dao.CalendarDao">

    <select id= "cList" parameterType="map" resultType="com.vo.CalendarVO">
        select * from calendar a
        INNER JOIN emp b ON a.emp_no = b.emp_no
        <where>
            AND a.emp_no = b.emp_no
        </where>
    </select>

    <delete id="delCalList" parameterType="map">
        DELETE FROM calendar
        <where>
            calendar_no = '${calendarNo}'
        </where>
    </delete>


    <insert id="addCalList" parameterType="map">
        INSERT INTO CALENDAR (CALENDAR_NO, EMP_NO, CALENDAR_ID, CALENDAR_TITLE, CALENDAR_CONTENT, CALENDAR_START, CALENDAR_END, REG_DATE, MOD_DATE)
        VALUES (calendar_pk_seq.nextval, 1003, 7, '${title}', '일정', '${start}', '${end}', to_char(sysdate,'YYYY-MM-DD HH24:MI:SS'), NULL)
    </insert>

    <update id="upCalList" parameterType="map">
        UPDATE calendar
        SET calendar_no = '${calendarNo}',
            calendar_title = '${title}',
            calendar_content = '예약',
            calendar_start = '${start}',
            calendar_end = '${end}'
        where calendar_no = '${calendarNo}'
    </update>

    <select id="calList" parameterType="map" resultType="map">
        SELECT
        a.reservation_no,
        a.reservation_title,
        a.reservation_start,
        a.reservation_end,
        b.asset_id,
        b.asset_no,
        b.asset_name,
        b.team_no,
        c.name
        FROM asset_reservation a
        INNER JOIN asset b ON a.asset_no = b.asset_no
        INNER JOIN emp c ON a.emp_name = c.emp_name
        <where>
            <if test="asset_no!=null">AND b.asset_no=#{asset_no}</if>
            <if test="gubun!=null">
                <choose>
                    <when test='gubun.equals("name")'>
                        AND c.name LIKE '%'||#{keyword}||'%'
                    </when>
                    <when test='gubun.equals("reservation_title")'>
                        AND a.reservation_title LIKE '%'||#{keyword}||'%'
                    </when>
                    <when test='gubun.equals("reservation_start")'>
                        AND a.reservation_start LIKE '%'||#{keyword}||'%'
                    </when>
                </choose>
            </if>
        </where>
        order by a.reservation_no desc
    </select>

    <select id= "myCalList" parameterType="map" resultType="com.vo.CalendarVO">
        SELECT
        *
        FROM calendar
        INNER JOIN asset b ON a.asset_no = b.asset_no
        INNER JOIN emp c ON a.emp_no = c.emp_no
        <where>
            AND a.asset_no = b.asset_no
            AND a.emp_no = c.emp_no
        </where>
    </select>
</mapper>